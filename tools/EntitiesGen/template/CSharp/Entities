/// <summary>
/// EntitiesGen から自動生成されます。直接編集しないでください
/// </summary>
using System;

namespace Entities
{
	{{ENUM}}
    /// <summary>
    /// {{COMMENT}}
    /// </summary>
	public enum {{NAME}}
	{
		{{MEMBERS}}
	}
	{{ENUM}}
	{{CLASS}}
    /// <summary>
    /// {{COMMENT}}
    /// </summary>
    [Serializable]
	public partial class {{NAME}}
	{
		{{MEMBERS}}
	}
	{{CLASS}}
}
